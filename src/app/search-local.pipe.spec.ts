import { SearchLocalPipe } from './search-local.pipe';

describe('SearchLocalPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchLocalPipe();
    expect(pipe).toBeTruthy();
  });
});
