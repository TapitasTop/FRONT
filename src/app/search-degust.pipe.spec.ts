import { SearchDegustPipe } from './search-degust.pipe';

describe('SearchDegustPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDegustPipe();
    expect(pipe).toBeTruthy();
  });
});
